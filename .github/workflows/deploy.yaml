name: Deploy

on:
  workflow_dispatch:
    inputs:
      namespace:
        description: OpenShift namespace
        required: true
        default: supply-chain-scenario-rag

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Manual deploy placeholder
        run: |
          echo "Deploy to OpenShift with: make deploy NAMESPACE=${{ inputs.namespace }}"
          echo "Requires oc login and HF token in helm values."
